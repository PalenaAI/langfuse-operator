apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "langfuse-operator.fullname" . }}-manager
  labels:
    {{- include "langfuse-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "langfuse-operator.fullname" . }}-manager
subjects:
  - kind: ServiceAccount
    name: {{ include "langfuse-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
